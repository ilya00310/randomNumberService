generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Numbers {
  id      Int         @default(autoincrement())         @id
  number  Int         
}